apiVersion: v1
kind: Service
metadata:
  name: {{ include "h2ogpt.fullname" . }}
  labels:
    {{- include "h2ogpt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "h2ogpt.selectorLabels" . | nindent 4 }}
