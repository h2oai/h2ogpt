apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "h2ogpt.fullname" . }}-test-connection"
  labels:
    {{- include "h2ogpt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "h2ogpt.fullname" . }}:{{ .Values.h2ogpt.service.port }}']
  restartPolicy: Never
